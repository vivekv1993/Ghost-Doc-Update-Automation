action_type: update_logline
log-line:
  id: "r"
  log_fields:
    log_field_groups:
      # 1. Target an existing group (Matches line ~140 in log-format.xml)
      - since: "4.3"
        standalone_fields:
          - id: "36"
            since: "22.6.0"
            doc: "TEST: This is a brand new field injected into the middle of the legacy 4.3 group."

      # 2. Append a brand-new group at the bottom of the 'r' log line
      - since: "22.6.0"
        doc: "TEST: Future release group tracking modern edge runtime definitions."
        standalone_fields:
          - id: "91"
            doc: "TEST: Standalone configuration metric inside our new cluster."
